# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Local temp / scratch (verification artifacts)
_*
*.tmp

# Local-only preview server (no-cache static server for iterating)
nocache_server.py

# Editor folders
.vscode/
.idea/
